
define command {
	command_name		check_dns
	command_line		$USER1$/check_dns -H $ARG1$ -a $ARG2$
}


define command {
	command_name		check_ports
	command_line		$USER1$/check_ports $ARG1$
}

define command {
	command_name		check_nrpe_connectivity
	command_line		$USER1$/check_nrpe -H $HOSTADDRESS$
}
