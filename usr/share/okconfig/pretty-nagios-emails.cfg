define command{
        command_name    perty-notify-service-by-email
        command_line    $USER1$/perty-nagios-mailer.pl "$CONTACTEMAIL$" "$LONGDATETIME$" "$NOTIFICATIONTYPE$" "$HOSTALIAS$" "$HOSTADDRESS$" "$SERVICEDESC$" "$SERVICESTATE$" "$SERVICEOUTPUT$" 
        }

